<?php require_once 'app/models/user.model.php' ?>
<?php require 'templates/layout/header.phtml' ?>

<?php $userModel = new UserModel(); ?>

<h2>Listado de Libros</h2>
<ul>
    <?php foreach($catalogue as $book): ?>
        <li> <h4> <a href="book/<?php echo $book->id ?>" class="link-body-emphasis"> <?php echo $book->nombre ?> </a> </h4> </li>
        <?php if(isset($_SESSION['ID_USER']) && $userModel->isAdmin($_SESSION['ID_USER'])): ?>

            <a href="deleteBook/<?php echo $book->id ?>" class="btn btn-outline-danger">Eliminar</a>

        <?php endif ?>
    <?php endforeach ?>
</ul>
    
<?php if(isset($_SESSION['ID_USER']) && $userModel->isAdmin($_SESSION['ID_USER'])): ?>
    <a href="showAddBook/" class="btn btn-primary">Agregar</a>
<?php endif ?>

<h2>Listado de Géneros</h2>
<ul>
    <?php foreach($genres as $genre): ?>
        <li> <h4> <a href="genre/<?php echo $genre->id ?>" class="link-body-emphasis"> <?php echo $genre->nombre ?> </a> </h4> </li>
        <?php if(isset($_SESSION['ID_USER']) && $userModel->isAdmin($_SESSION['ID_USER'])): ?>

            <a href="deleteGenre/<?php echo $genre->id ?>" class="btn btn-outline-danger">Eliminar</a>

        <?php endif ?>
    <?php endforeach ?>
</ul>

<?php if(isset($_SESSION['ID_USER']) && $userModel->isAdmin($_SESSION['ID_USER'])): ?>
    <a href="showAddGenre/" class="btn btn-primary">Agregar</a>
<?php endif ?>

<?php require 'templates/layout/footer.phtml' ?>
